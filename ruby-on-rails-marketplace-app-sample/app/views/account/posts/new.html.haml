= content_for :account_nav_bar_content do
	%strong New Blog Post Submission
	%span Submit your insights for publishing on #{ current_site.name }.

= content_for :content do
	= render 'layouts/header'
	= render 'post_form'
	= render 'layouts/footer_hero'
	= render 'layouts/footer'

= render :file => 'layouts/application'
